node_modules/
dist/
out/
build/

# Secrets et configuration locale — ne jamais versionner
.env
.env.*
!.env.example

# Artefacts de collecte et de build
scripts/rapport-collecte-directe.json
*.log
.DS_Store
niveaux-pays.json

# Mémoire inter-runs du job de collecte planifiée (SRC_HEALTH /
# PROXY_HEALTH_PAYS) — restaurée/sauvegardée via actions/cache, jamais
# commitée (voir scripts/lib/sante-collecte.js).
.cache-sante/
